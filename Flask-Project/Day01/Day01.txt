1.实现 user 业务逻辑包
	http://localhost:5000/user
	执行 user 中的视图 ：这是 user 中的首页

	使用蓝图(Blueprint)配置
2.将Blog项目按照项目结构搭建起来


<li class="timer">{{ topic.pub_date.strftime("%Y-%m-%d") }}</li>
strftime("%Y-%m-%d")

{% for x in Y%}
    #存在内置变量loop，拿过来可以直接用
    loop.inedx #表示遍历的次数
{% endfor%}